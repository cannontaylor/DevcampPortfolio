<div class="masthead clearfix">
  <div class="inner">
    <h3 class="masthead-brand">Cannon Taylor</h3>
    <nav class="nav nav-masthead">
      <%= link_to "Home", root_path, class: "#{cp(root_path)} nav-link" %>
      <%= link_to "About Me", about_me_path, class: "#{cp(about_me_path)} nav-link" %>
      <%= link_to "Contact", contact_path, class: "#{cp(contact_path)} nav-link" %>
      <%= link_to "Blog", blogs_path, class: "#{cp(blogs_path)} nav-link" %>
      <%= link_to "Portfolio", portfolios_path, class: "#{cp(portfolios_path)} nav-link" %>
      <%= login_helper 'nav-link' %>
    </nav>
  </div>
</div>